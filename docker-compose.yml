services:
  api:
    build: .
    ports:
      - "4000:4000"
    command: ["sh", "-c", "./src/bin/src start"]
    environment:
      DATABASE_URL: ${DATABASE_URL}
      SECRET_KEY_BASE: ${SECRET_KEY_BASE}
      PHX_SERVER: true
    restart: always

volumes:
  db_data:

